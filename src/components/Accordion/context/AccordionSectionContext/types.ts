export type AccordionSectionContextProviderProps = {
  sectionId: string;
  isSelected: boolean;
  isExpanded: boolean;
};

export type AccordionSectionContextType = {
  sectionId: string;
  isSelected: boolean;
  isExpanded: boolean;
};
